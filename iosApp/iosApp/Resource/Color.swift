//
//  Color.swift
//  iosApp
//
//  Created by Anantasak on 25/10/2564 BE.
//  Copyright © 2564 BE PSM. All rights reserved.
//

import SwiftUI

extension Color {
     
    static let bgDark = Color(UIColor(named: "bgDark") ?? UIColor.systemBackground)
    static let shadowColor = Color(UIColor(named: "shadowColor") ?? UIColor.systemBackground)
    static let progressColor = Color(UIColor(named: "progressColor") ?? UIColor.systemBackground)
}
